# project_newShareCFTGenerator

PowerShell utilities for generating CloudFormation templates (migrated from legacy archive).

Origin: internal migration. Inspect scripts and test before running in production.
